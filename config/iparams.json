{
  "kwtsms_username": {
    "display_name": "kwtSMS API Username",
    "description": "Your kwtSMS API username (letters and numbers only, no spaces)",
    "type": "text",
    "required": true,
    "regex": {
      "username-format": "^[a-zA-Z0-9]+$",
      "username-format-error": "Username must contain only letters and numbers (a-z, A-Z, 0-9). No spaces, dashes, or special characters."
    }
  },
  "kwtsms_password": {
    "display_name": "kwtSMS API Password",
    "description": "Your kwtSMS API password",
    "type": "text",
    "required": true,
    "secure": true
  }
}
